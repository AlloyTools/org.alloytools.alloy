subprojects {

    apply plugin: "java"

    compileJava {
        options.encoding = "UTF-8"
        sourceCompatibility = 1.8
        targetCompatibility = 1.8
    }

    repositories {
        mavenCentral()
    }
    dependencies {
        compile "junit:junit:4+"
    }
}